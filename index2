
<!DOCTYPE html>
<html lang="bn">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Color City Ltd — App Launcher</title>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Hind+Siliguri:wght@400;500;600;700&display=swap" rel="stylesheet"/>
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
:root{
  --navy:#0A1A2F;--blue:#143C5C;--accent:#2E86C1;
  --hdr-line:#C9A65A;
  --bg:#F0F4F8;--card:#fff;--border:#DDE3EA;
  --text:#1A202C;--muted:#64748B;
  --radius:16px;
  --shadow:0 2px 12px rgba(13,27,42,.09);
  --shadow-lg:0 16px 44px rgba(13,27,42,.18);
}
body{
  font-family:'Inter','Hind Siliguri',sans-serif;
  background:
    radial-gradient(1200px 600px at 15% -10%, rgba(46,134,193,.25), transparent 60%),
    radial-gradient(900px 500px at 100% 0%, rgba(201,166,90,.18), transparent 55%),
    linear-gradient(160deg,var(--navy) 0%,var(--blue) 55%,#1B4F72 100%);
  min-height:100vh;
  color:var(--text);
  display:flex;
  flex-direction:column;
  align-items:center;
  padding:48px 20px 32px;
}
.top{
  text-align:center;
  margin-bottom:44px;
}
.top .eyebrow{
  color:var(--hdr-line);
  font-size:12px;
  letter-spacing:3px;
  text-transform:uppercase;
  font-weight:600;
  margin-bottom:10px;
}
.top h1{
  color:#fff;
  font-size:30px;
  font-weight:700;
  letter-spacing:.3px;
}
.top p{
  color:rgba(255,255,255,.62);
  font-size:14px;
  margin-top:8px;
}
.cards{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(260px,1fr));
  gap:26px;
  max-width:820px;
  width:100%;
}
.card{
  background:var(--card);
  border-radius:var(--radius);
  box-shadow:var(--shadow);
  padding:34px 28px;
  text-decoration:none;
  color:var(--text);
  display:flex;
  flex-direction:column;
  align-items:center;
  text-align:center;
  gap:14px;
  border:1px solid var(--border);
  border-top:4px solid var(--accent);
  transition:transform .22s ease, box-shadow .22s ease, border-color .22s ease;
  position:relative;
  overflow:hidden;
}
.card:hover{
  transform:translateY(-6px);
  box-shadow:var(--shadow-lg);
}
.card .icon{
  width:64px;height:64px;
  border-radius:18px;
  display:flex;align-items:center;justify-content:center;
  font-size:30px;
  background:linear-gradient(135deg,var(--navy),var(--blue));
  color:#fff;
  box-shadow:0 6px 18px rgba(20,60,92,.35);
}
.card.dash .icon{
  background:linear-gradient(135deg,#1f4c31,#2f6b45);
  box-shadow:0 6px 18px rgba(31,76,49,.35);
}
.card .name{
  font-size:19px;
  font-weight:700;
  color:var(--navy);
}
.card .desc{
  font-size:13px;
  color:var(--muted);
  line-height:1.5;
}
.card .go{
  margin-top:10px;
  font-size:12.5px;
  font-weight:600;
  color:var(--accent);
  display:flex;
  align-items:center;
  gap:6px;
}
.card.dash .go{color:#2f6b45}
.card .go .arrow{transition:transform .18s ease}
.card:hover .go .arrow{transform:translateX(4px)}
.foot{
  margin-top:48px;
  color:rgba(255,255,255,.4);
  font-size:12px;
  text-align:center;
}
</style>
</head>
<body>

<div class="top">
  <div class="eyebrow">Color City Ltd</div>
  <h1>অ্যাপ লঞ্চার</h1>
  <p>যে অ্যাপে কাজ করতে চান, সেটি সিলেক্ট করুন</p>
</div>

<div class="cards">
  <a class="card" href="kaizen.html">
    <div class="icon">📋</div>
    <div class="name">Kaizen</div>
    <div class="desc">Multi-Team Continuous Improvement System — টিম লগইন করে কাইজেন এন্ট্রি, ট্র্যাকিং ও রিপোর্ট দেখুন।</div>
    <div class="go">ওপেন করুন <span class="arrow">→</span></div>
  </a>

  <a class="card dash" href="Production_Dashboard.html">
    <div class="icon">📊</div>
    <div class="name">Production Dashboard</div>
    <div class="desc">প্রোডাকশন ড্যাশবোর্ড — লগইন করে প্রোডাকশন ডেটা, চার্ট ও রিপোর্ট এক্সেস করুন।</div>
    <div class="go">ওপেন করুন <span class="arrow">→</span></div>
  </a>
</div>

<div class="foot">Choose an app above — each works independently with its own login.</div>

</body>
</html>
